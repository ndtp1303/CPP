#include <bits/stdc++.h>

using namespace std;

#define un unsigned
#define ll long long

un ll a, b, c;

int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> a >> b >> c;
    
    cout << c - b - 1;

    return 0;
}