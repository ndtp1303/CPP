#include <bits/stdc++.h>

using namespace std;

float n;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> n;

    printf("%0.1f", (n / 4) * (n / 4));
}